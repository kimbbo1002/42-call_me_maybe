[project]
name = "CMM"
version = "1.0.0"
description = "Introduction to function calling in LLMs"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic",
    "numpy",
    "torch",
    "transformers"
]
[tool.uv.sources]
llm-sdk = { path = "./llm_sdk" }